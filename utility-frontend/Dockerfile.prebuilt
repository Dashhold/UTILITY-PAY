FROM nginx:1.27-alpine

# Copy prebuilt dist
COPY dist-prebuilt /usr/share/nginx/html

# Copy nginx server config to conf.d
COPY docker/nginx-app.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

# No custom command needed: nginx:alpine runs nginx by default
