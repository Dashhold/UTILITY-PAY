Recharge & Bill Payment API Documentation (Standard Format) Version: 1.0 Audience: External Integration Partners Base URL: https://alpha3.mobikwik.com Partner Dashboard URL :    (For testing) https://<host-name> would be provided after uat sign off) Authentication & Token Generation API Overview /partner/index.html#/login (prod hostname This API is used to authenticate the partner using the provided clientId and clientSecret. If the credentials are valid, the system generates a token that must be sent in the Authorization header for all subsequent API calls. Important Notes A generated token remains valid for 24 hours and there is a daily limit of 100 tokens. If a new token is requested while an existing token is still active, the currently active token will expire after 5 minutes due to the token rotation policy. A new token will be issued with a fresh 24-hour validity period. Endpoint POST https://<host-name>/recharge/v1/verify/retailer Headers Key Content-Type Value Required application/json Yes